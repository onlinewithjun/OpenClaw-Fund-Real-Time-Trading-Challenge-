# 升级日志（2026-03-12）

## 一、执行门控与候选标的归一化
- 加强了 14:00 预案与 14:45 执行门控的脚本硬校验。
- 增加 14:40 一致性补刷标记，再进入最终发布。
- 执行侧新增“仅回落试错”约束，禁止盲目追涨。
- 可执行标的统一到场外可买口径。
- 增加从场内代码 `159509` 自动映射到场外 `019118` 的兜底逻辑。

## 二、扩池与规则刷新增强
- 扩池输出新增 added/removed 明细，便于追踪池子变化。
- 规则元数据刷新，`instrument_rules.json` 时间戳与来源映射保持同步。
- 复盘与预案脚本输出增强硬门控上下文，便于复核。

## 三、状态准确性修复（根因补丁）
- 修复了“用户端已更新，但系统份额/成本底账未同步”导致的持仓漂移。
- 新增 `pendingTransactions` 在途交易模型，避免买卖在途被遗漏。
- `execution_receipt_updater.py` 增强：
  - 对未确认份额的买/卖回执写入在途状态；
  - 对未显式回传现金的 BUY，采用保守现金处理。
- 新增 `reconcile_positions.py`：支持用人工快照（持仓金额/持仓盈亏）反推并回写份额与成本。
- `review_summary_script.py` 增强：单列 BUY/REDEEM 在途，不再混入已确认持仓归因。

## 四、数据口径澄清
- 净值快照仍来自天天/东方财富基金接口（`dwjz` 优先，`gsz` 兜底）。
- 本次问题主因不是净值来源错误，而是状态账本与实盘底账错位。
- 已在 2026-03-12 执行人工对账回写，使系统状态与用户确认口径对齐。

## 五、文档同步
- 已刷新 `README.md` 与 `README.zh-CN.md`。
- 新增 2026-03-12 中英升级日志入口。
- 中文 README 的 prompts 与 cron 时间已与当前运行流程对齐。
