
## 2026-03-11 定时任务体系重构与稳定性修复（关键记录）

- 完成基金挑战与资讯任务的大规模重构：
  - 基金挑战任务统一加签名与编号（#01~#08），并统一中文优先推送。
  - 资讯任务拆分并明确优先链路：
    - 资讯#03 改为 A股晚报（直连优先，超时切代理）。
    - 新增资讯#04 港股晚报（代理优先，超时切直连），时间设为 22:40。
  - OpenClaw 周报任务改名为 OpenClaw#01（与基金挑战解耦）。

- 删除了全部历史禁用/失效 cron 任务（8 条），当前仅保留有效任务集（13 条）。

- #02（基金挑战扩池刷新）关键演进：
  1) 先从“依赖 daily_candidates.md”切换到 json 真源。
  2) 用户明确要求“真联网扩池”后，已升级为真实联网扫描与精筛：
     - 在线扫描约 57+ 基金，精筛 12。
     - 自动写入 added/removed/retained。
     - updatedAt/verifiedAt 当日刷新。
     - 输出 source=online-json。
  3) 只对 #02 禁用代理后，超时问题显著改善（多次手动执行成功，秒级~几十秒）。

- 决策机制升级：
  - execute gate 增加卖出机制（exitConsensus + 无现金时先 REDEEM）。
  - 输出规则改为：BUY 按金额；REDEEM 按份额。
  - 卖出参数改为激进版（15% PV，100% cap）。

- 稳定性/可观测性改进：
  - 要求所有任务成功/失败都推送 Telegram，失败带原因。
  - 加入“任务超时”通知要求。
  - 资讯任务加入“代理失败/超时 -> 切换另一链路重试”的硬逻辑文案。

- OpenClaw#01 超时根因定位：
  - 不是脚本慢，也不是网络；脚本本地仅约 14ms。
  - 超时点在 isolated + agentTurn 编排链路（会话启动/模型回合前置）。
  - 已改为 main + systemEvent 直跑，恢复稳定。

- 过程反思（用户明确反馈）：
  - 用户要求以后遇到超时问题应先自主完成端到端 RCA，再汇报，不要让用户逐步指挥。
  - 已记录为工作流程改进项（超时RCA固定检查清单）。
