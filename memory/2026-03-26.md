# 2026-03-26

## 基金挑战#17 策略复查
- 发现候选池存在同一基金不同份额（如 A/C）重复占位，导致 `gate_scoring.py`、evidence 候选评分和执行选标都把同一机会重复计数，虚增候选深度、`strongSwitchCount` 与防御覆盖。
- 已做低风险可逆修正：
  - `fund_challenge/scripts/gate_scoring.py`：按基金主体去重 share class，再计算 gate 统计；
  - `fund_challenge/scripts/build_evidence.py`：候选评分列表去重，避免复盘/审计被 A/C 重复误导；
  - `fund_challenge/scripts/execute_gate_script_only.py`：执行选标前去重；若同主题里已有持仓份额，优先保留与现有仓位一致的份额，减少噪音切换。
- 验证结果：语法检查通过；重新生成 evidence 后 `candidateCount` 50→48，去掉了黄金联接 A/C 与中欧时代先锋 A/C 的重复机会，统计口径更真实。
- 当前策略结论：真正短板仍是仓位质量与集中度，而不是“机会太少”；去重后能减少系统把重复候选误判为更强 edge 的问题。
