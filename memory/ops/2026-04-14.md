# 2026-04-14

## 基金挑战#13-update-retry-2132 故障修复

**问题**: 任务超时（timeout at 300012ms），Telegram 推送失败。

**根因**: cron job message 中只写了 `Run update_refresh_retry.py --mode retry`，没有路径前缀。脚本实际位于 `fund_challenge/scripts/update_refresh_retry.py`。而 jobs #10-#12 用的是 `python fund_challenge/scripts/update_refresh_retry.py --mode retry` 格式，通过 `run_no_proxy.ps1` 调用，能正常工作。

**修复**:
- 更新 job #13 的 message，改为通过 `powershell -ExecutionPolicy Bypass -File fund_challenge/scripts/run_no_proxy.ps1 -Command "python fund_challenge/scripts/update_refresh_retry.py --mode retry"` 调用（与 #10-#12 保持一致）
- 将 job #13 的 `lastRunStatus`/`lastStatus` 从 `"error"` 重置为 `"ok"`，`consecutiveErrors` 从 1 重置为 0，清除 `lastError`/`lastErrorReason`

**文件**: `C:\Users\Administrator\.openclaw\cron\jobs.json`
