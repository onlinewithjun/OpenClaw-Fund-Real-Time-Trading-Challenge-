## 2026-03-29
- 01:20 基金挑战#17 夜间策略复查：当前 `state.asOf=2026-03-27 14:57`，周末仅适合做系统优化，不能把旧盘中状态当成新交易日信号。
- 本轮低风险可逆优化：
  - `universe_refresh_script_only.py` 改为优先从 `fund_challenge/state.json` 识别“当前持仓”，不再把历史已买过但已退出的代码误当作现持仓强制保留；已实测刷新后将误留的 `019118` 从候选池移除。
  - 将 `基金挑战#01-healthcheck-0900` cron 从 `main/systemEvent` 改为 `isolated/agentTurn + announce`，并缩短超时到 180 秒，降低主会话阻塞/超时概率，便于早盘快速发现真实阻塞项。
- 复盘结论：当前主要短板仍是旧状态滞后 + 候选池对历史标的残留偏好，而不是安全边界过严；本轮修正提升的是执行洁净度与运维稳定性，没有放松人工确认边界。
