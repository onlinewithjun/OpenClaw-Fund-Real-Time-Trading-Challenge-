## 2026-03-23
- 01:20 基金挑战夜间策略复查：修补 `fund_challenge/scripts/gate_scoring.py` 的硬回撤漏洞。此前在组合回撤已跌破 `-5%` hard stop 时，只要 `strongSwitchReady=true`，系统仍可能把 `riskSwitchComputed` 判成 `risk_on`，并给出试单 BUY 信号；现改为 hard stop / emergency pause 优先级最高，强势切换不能覆盖组合级风控，只有 `drawdownPass=true` 时才允许 offensive `risk_on`/trial buy。
- 同次复查优化 `fund_challenge/scripts/review_summary_script.py`：夜间若拿到 stale `state.json`，不再只返回笼统 `stale_state_data`，而是明确输出 `state.asOf` 与当前在途单数量/代码，提醒“此时只能做系统优化，不能把夜间 stale state 当成新交易日有效盘后结论”。
- 当前观察：`state.asOf` 仍停留在 2026-03-20 21:45，且有 1 笔 `020899` 在途 BUY；说明真正的执行质量瓶颈仍是结算/落账闭环慢于信号节奏，后续应优先围绕在途单管理与次日盘中 freshness 做优化，而不是夜间继续放宽买入。 
