## 2026-04-06

### 🌙 01:20 基金挑战夜间策略复查
- `memory_search` 本轮不可用（embedding/provider fetch failed），已改为直接复读 `MEMORY.md`、近两日日记与 `memory/finance/2026-03-27-fund-challenge-lessons.md`。
- 核查结果：pending 阻塞逻辑未见新的机械性错误；当前 `000056` BUY 在正常确认窗口内，不属于需要夜间强制修复的 stale blocker。
- 发现真实策略偏差：`daily_candidates.json` 中多只支付宝可买基金被错误回落到 `broad_index_core` 默认分类，导致 gate scoring 的 defensive coverage、category bias 与候选排序失真，属于会持续污染短线决策的低风险系统缺陷。
- 已修复 `fund_challenge/scripts/universe_refresh_script_only.py`：优先使用 `universe/alipay_allowed.json` 的人工确认分类，保留 bond_primary / bond_secondary / index_enhanced / manufacturing 等细分类，并同步扩展 score bias 与 category cap。
- 已立即校正当前 `fund_challenge/universe/daily_candidates.json` 中 000054 / 000171 / 000311 / 001323 / 001869 / 002610 的分类，避免下一轮 gate scoring 继续在错误语义上做判断。
- 结论：当前系统的主要短板不是 safety rail 太强，而是候选语义层脏数据会把量化模块带偏；先修分类质量，比盲目加大仓位或追更热赛道更能提升短线胜率。
