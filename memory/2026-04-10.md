# 2026-04-10 日记

## 基金挑战
- 01:20 策略复盘：当前 state 正常，PV 约 1111.32（21:45 state 为 1083.86），现金 475.13，pendingTransactions=0，未见新的机械阻塞。
- 检查通过：同代码 pending / 现金不足才阻塞；支付宝可买白名单仍在执行；evidence 中 Macro 30 / Sentiment 25 / Sector 25 / Quant 20 框架仍被保留。
- 低风险优化 1：`universe_refresh_script_only.py` 将 recent redeem 候选冷却从 2 天延长到 3 天，减少刚卖又买回的低质量 churn，但不影响仍在持仓的代码。
- 低风险优化 2：`status_brief.py` 的调仓建议不再把近 3 天刚赎回标的当作最佳新加仓候选，避免摘要层误导。
- 低风险优化 3：`fund_challenge/prompts/review.md` 加入显式复盘检查项，强制复核 pending guard、支付宝约束、决策权重和 recent redeem churn 风险。
- cron 优化：基金挑战#12/#13/#14 update-retry 统一把 `UPDATE_RETRY_SKIP no_active_retry` 视为静默成功，减少 21:24 之后的无意义噪音推送。
- 安全边界未变：真实 BUY/REDEEM 仍需人工确认，不绕过外部平台执行边界。

---

## 🔐 安全扫描报告 — 2026-04-10 01:50

**触发**: workspace-secret-scan-daily cron

**本次发现（NEW — 2026-04-05 incident 后续）**:

本次扫描在工作区根目录发现多个之前未纳入扫描范围的临时调试脚本，包含真实 Telegram bot tokens 和 163 邮箱密码，已全部完成本地 redaction：

受影响的已修复文件（本地 redaction 已完成）：
- `send_msg.py` — Telegram bot tokens（第 1 个和第 2 个均已脱敏）
- `send_telegram.py` — Telegram bot token（第 2 个 token）
- `send_telegram.ps1` — Telegram bot token（第 1 个 token，已作为 fallback）
- `do_telegram.ps1` — Telegram bot token（第 1 个 token）
- `test_163_imap.py` — 163 邮箱密码
- `test_163_imap2.py` — 163 邮箱密码
- `test_163_imap3.py` — 163 邮箱密码（LOGIN 明文）
- `test_163_imap4.py` — 163 邮箱密码（TLS 1.2 调试）
- `test_163_imap5.py` — 163 邮箱密码（多端口调试）
- `TOOLS.md` — Google OAuth client_secret + refresh_token（本次新发现，已脱敏）

**本地 redaction 状态**: ✅ 已全部完成

**Git 历史风险**: ⚠️ 这些文件此前已提交至 git 历史；Telegram tokens 仍在有效期内

**🔴 需人工立即处理**:
1. **Telegram tokens** — 在 Telegram BotFather 访问 `/revoke` 撤销这两个 bot tokens：
   - `7816897827:AAFDiwioHWMm1RwDV7UuC3N3KNFVqDVF8gQ`
   - `8108923588:AAEQ4d1UU7qTquGqVI-JMpLFwhLxO68`
   并生成新 token 在 OpenClaw config 中更新
2. **163 邮箱密码** `JErYSeGxhEWbivKY` — 立即在 163 邮箱后台修改密码
3. **Git 历史清理** — 运行 `git filter-repo` 清除上述文件的 git 历史，并 `git push --force`
4. **Gmail refresh_token** — 在 Google Cloud Console 撤销该 OAuth token 并重新授权

**已知持续风险**（自 2026-04-05 未解决）:
- `skills/code/xiaohongshu-mcp/cookies.json` — git HEAD 仍含原始小红书 session cookies；需运行 `git filter-repo` 清除文件历史

**扫描结论**: 本次新增 10 个文件共 11 枚真实凭据，已全部本地脱敏。Git 历史中的泄露风险仍需人工清理。
