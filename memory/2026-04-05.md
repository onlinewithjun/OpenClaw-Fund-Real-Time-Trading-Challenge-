# 安全事件记录 — 2026-04-05

## 🔴 事件摘要

**时间**: 2026-04-05 01:50 (Asia/Shanghai)  
**触发**: workspace-secret-scan-daily cron  
**影响文件**:
- `skills/code/xiaohongshu-mcp/cookies.json` — git HEAD 中包含小红书真实会话 Cookies

**暴露内容** (git HEAD，未加密存储):
- `web_session`: `0400698f10f79c56d9dbbfc6903b4bf465fa82`
- `id_token`: `VjEAAC8g7AozUmQ/W0qqd+Z8ZJm4A2oL...` (JWT)
- `websectiga`: `7750c37de43b7be9de8ed9ff8ea0e576519e8cd2157322eb972ecb429a7735d4`
- `a1`, `acw_tc`, `webId`, `gid`, `sec_poison_id` 等多枚 Cookies

**工作区状态**:
- 工作树中 cookies.json 已部分 redacted（值为 `[REDACTED]`）
- 但 git HEAD 仍包含原始未脱敏数据
- `.gitignore` 已包含 `*cookies*.json`，但文件已 committed，无法保护

**风险等级**: 🔴 高
- 小红书 `web_session` + `id_token` 可用于会话劫持
- `websectiga` 为小红书安全签名 Cookie，可用于绕过风控
- Git 历史一旦泄露（GitHub public fork、备份外泄），凭证永久有效

## ✅ 已执行操作

- [x] 工作树确认已部分脱敏（值替换为 `[REDACTED]`）
- [x] 扫描 workspace 其他 md/json/py/sh 配置文件，无其他高风险凭证
- [x] 写入本 incident 记录

## ⚠️ 待执行操作（需人工）

1. **立即小红书账号重新登录**，触发所有现有 session 失效（web_session、id_token 轮换）
2. **清空本地小红书浏览器登录状态**，重新扫码登录
3. **从 git 历史中永久删除 cookies.json**：需要运行 `git filter-repo --path skills/code/xiaohongshu-mcp/cookies.json --force`，然后 `git push --force`
4. **确认 .gitignore 生效**：将 cookies.json 彻底从 git 跟踪中移除
5. **检查 OpenClaw workspace 是否有 push 到远程**：若有远程仓库，应假设 git 历史已暴露，立即通知用户

## 扫描范围

扫描了 git HEAD 中所有文本文件（md/json/py/sh/txt/yml/yaml/toml/ini/conf/ps1/js/ts）：
- ✅ `TOOLS.md`, `AGENTS.md`, `MEMORY.md`, `USER.md`, `SOUL.md`, `IDENTITY.md`, `HEARTBEAT.md` — 无凭证
- ✅ `fund_challenge/state.json` (HEAD) — 仅有通用关键词，无凭证值
- ✅ `agents/routing.json` (HEAD) — 无凭证
- ✅ `memory/secret-handling-rules.md` (HEAD) — 仅文档内容
- ✅ `fund_challenge/decision_history.jsonl`, `ledger.jsonl` — 无凭证
- ✅ 其他 skills/*.md, agents/*.md, memory/ 日常记录 — 无凭证
- 🔴 `skills/code/xiaohongshu-mcp/cookies.json` (HEAD) — **已确认为唯一高风险文件**

---

## 📝 Memory 维护记录 (2026-04-05 03:40)

- **归档**: `memory/2026-03-29.md` → `memory/archive/`（8天，超7天阈值）
- **归档**: `memory/ops/routing-metrics.json` → `memory/archive/ops/`（2026-03-16过时指标，无运营价值）
- **索引**: `memory/ops/pending-telegram-alert.md` 保留原位，内容含待人工发送的 Telegram 安全告警
- **MEMORY.md**: 已审核，紧凑，无需调整
- **daily notes 窗口内**: 2026-03-30 ~ 2026-04-05（共7个），无需进一步归档
- **提交**: `b56f8d8`，仅 memory 相关变更
