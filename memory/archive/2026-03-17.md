- 用户授权：今后这类低风险异常默认由助手自行排查、修复，不需要先询问。
- 2026-03-17 10:53 修复基金挑战健康检查异常：新增 `fund_challenge/scripts/auto_close_stale_pending.py`，自动关闭超过 48h 的 stale pending transactions 并写入 ledger；`healthcheck_brief.py` 先执行该自愈逻辑再判断。
- 本次自愈结果：关闭 stale pending `019118`、`017192`；随后 `HEALTHCHECK_OK TRADING_DAY_OK`。
- 备注：本次 memory_search 不可用（embedding provider 401 invalid_api_key），需后续排查 embeddings 配置。
- 用户新增明确偏好：所有定时任务执行结果都要投递到 Telegram 7107266459。
- 2026-03-17 11:13 已统一修正 cron delivery：为现有定时任务补齐/修正 Telegram announce 投递目标；并把遗留 `agentId=main` 的资讯任务切回 `main-agent`，避免“执行了但没发到 Telegram”。
- 2026-03-17 14:38-14:42 单 agent 回滚后的 cron/Telegram 修复：定位到大量 cron 仍写入 `agent:main-agent:main`，导致主路径 systemEvent 执行但不投递到 Telegram。已将基金挑战主路径任务的 `sessionKey` 改回 `agent:main-agent:telegram:direct:7107266459`，并对 `C:\Users\Administrator\.openclaw\cron\jobs.json` 做了残留 `agent:main-agent:main` 的全量替换。补跑结果：`#01 健康检查` 先报旧 marker 告警（stale consistency marker: 2026-03-16T14:40:21+08:00），随后 `#04b 14:40一致性补刷` 成功（asOf=2026-03-17T14:40:33+08:00, candidates=9），说明主要是时序假阳性，路由已基本恢复。
- 2026-03-17 15:10-15:32 继续排查后确认 `C:\Users\Administrator\.openclaw\cron\jobs.json`、`jobs.json.bak`、`jobs.json.repair-20260317-1459.bak` 均已损坏为非法 JSON；同时发现 cron 存储被批量污染（`RECOVERED_SYSTEM_EVENT` / `RECOVERED_AGENT_TURN` 占位内容、`@heartbeat` 错投递、中文内容导致 JSON 字符串损坏）。已通过 `cron.update` 先恢复基金挑战主链路 #01-#08 的真实任务正文与 Telegram 直达 `sessionKey=agent:main-agent:telegram:direct:7107266459`，随后备份坏文件为 `C:\Users\Administrator\.openclaw\cron\jobs.json.pre-rebuild-20260317-1532.bak`，并重建了一份 **ASCII-only** 的干净 `jobs.json`（优先保留基金挑战主链路与 4 个资讯任务），用以绕过本机中文编码写坏 JSON 的问题；重建后的 `jobs.json` 已通过 `ConvertFrom-Json` 校验为 `VALID_JSON`。当时 gateway restart 已发起，主目标是恢复 cron 存储可解析、可重启、可继续投递 Telegram。
