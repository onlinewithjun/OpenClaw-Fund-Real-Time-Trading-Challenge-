## 2026-03-22

### 基金挑战夜间策略复查 (01:20)
- 发现执行层还有一个真实风控穿透漏洞：`execute_gate_script_only.py` 虽已读取 `adjustedSuggestedBuyPct`，但 `compute_trial_amount()` 会把 <=5% 的仓位重新抬回 5%，导致 drawdown 分层在 `0.00` / 超低仓位场景下仍可能被强制恢复为最小试仓。已修复：当调整后仓位 <=0 时直接返回 0，并在主流程改为 `HOLD`，不再把 hard stop / emergency pause 伪装成 20 元试仓。
- 发现 21:45 复盘链路存在确定性报错：`review_summary_script.py` 使用了未定义变量 `active_pending`，会让晚间复盘 cron 直接失败。已补充 `active_pending_transactions()` 并统一忽略已 settled/cancelled 或带 `resolvedAt` 的旧在途单。
- 同步优化夜间复盘口径：增加“账面PV / 含BUY在途经济PV”双口径，避免 BUY 在途先扣现金、未入持仓时把结算时差误判成新的策略恶化。
- 当前复查结论未变：短线 edge 仍偏向通信等强势科技方向，但 `017192` 仍是主要拖累；`020899` 买单落账前应继续抑制无效换手。

### 🔒 工作区秘密扫描事件 (01:50)
- **扫描范围**: git-tracked 文本文件及常见配置文件 (*.md, *.json, *.txt, *.py, *.js, *.ts, *.sh, *.ps1, *.yaml, *.yml, *.env)
- **扫描模式**: Telegram bot tokens, API keys, GitHub tokens, private keys, bearer tokens, webhook secrets, session cookies
- **发现**: 
  - `skills/code/xiaohongshu-mcp/cookies.json` 包含小红书会话认证 cookies（含 `id_token`, `web_session`, `a1` 等敏感字段）
- **处置**:
  - 已立即 redact 所有敏感 cookie 值为 `[REDACTED]`
  - 已更新 `.gitignore` 添加 `*cookies*.json` 模式防止再次提交
  - 建议用户重新登录小红书获取新会话 cookies（如该技能仍需使用）
- **其他**: 未发现 Telegram bot token、GitHub token、私钥、API key 等其他高危凭证泄露

### 🧹 Memory 维护 (03:40 cron)
- 归档过期 daily notes：`2026-03-14.md`、`2026-03-15.md` → `memory/archive/`
- `MEMORY.md` 保持紧凑，无需改写
- 其余规则文档、脚本、反思文件均保留（仍具 operational 价值）
