# 2026-03-14

## OpenClaw 维护记录
- 已验证并放行 `openai-codex/gpt-5.4`，默认主模型已切换为 `openai-codex/gpt-5.4`。
- 修复 gateway service 路径漂移：重新安装 Scheduled Task，服务入口已从旧 npm 全局路径切到当前 nvm 安装路径。
- 清理 session store：
  - 删除 11 条 transcript 缺失的脏索引；
  - 删除 39 个 orphan transcript 文件；
  - `openclaw doctor` 已不再报告 gateway entrypoint mismatch / missing transcripts / orphan transcripts。

## Memory 管理决策
- 用户明确同意：
  1. 记录本次维护到 memory；
  2. 审视并清理全部 memory；
  3. 实现“上下文快用完时自动记录 memory”的能力。
- 已决定采用“行为级自动化”方案：在 `AGENTS.md` 中加入高水位规则，要求在上下文达到高占用时，优先把关键状态写入 `memory/YYYY-MM-DD.md` 与必要的 `MEMORY.md`，再继续对话或等待压缩。
- 暂不做侵入式代码级改造；优先用 workspace 规则固化该习惯，因为可立即生效且风险最低。
- 用户进一步授权：以后在**不影响体验**前提下，允许助手主动做最大限度的优化；不需要对每个低风险优化反复询问。

## Memory 清理结论
- `MEMORY.md` 可继续压缩：保留身份、偏好、基金挑战结论、关键基础设施状态即可；把过程性细节留在 daily memory。
- 已将旧 daily memory 归档到 `memory/archive/`。
- 进一步做了保守型专项清理：将 `report-2026-03-05.md`、`report-2026-03-06.md`、`session-wrapup-2026-03-06.log`、`memory-maintenance-schedule.md` 移入 `memory/archive/special/`。
- 保留在根目录的专项文档主要是仍具规则/反思价值的内容，例如基金推荐错误反思、独立交易说明、持仓规则与 memory 维护脚本。

## 基金挑战修复（2026-03-14 16:xx）
- 已修复 `update_refresh_retry.py` 只有 init 没有 retry 调度的问题：新增 #06b~#06f 五个晚间更新重试 cron（21:08/21:16/21:24/21:32/21:40）。
- 已升级 `healthcheck_brief.py`：不再只检查文件/语法，还会校验 `state.json`、`daily_candidates.json`、`consistency_04b.json` 在关键时间窗内的新鲜度；手动验证时已成功识别 `state.asOf` 为昨日数据并告警。随后又补充了“非交易日友好模式”：周末/非交易日返回 `HEALTHCHECK_OK NON_TRADING_DAY ...`，不再制造误报。
- 已新增 `fund_challenge/scripts/maintenance_cleanup.py`，并将 #08 维护 cron 切换到该脚本；维护范围扩展为 runtime cache prune、out 临时失败文件清理、旧 evidence 归档、`__pycache__` 清理、过旧 runtime snapshot 清理。随后已清掉历史嵌套残留目录 `fund_challenge/fund_challenge/out`。
- 已把 `execute_gate_script_only.py` 中的临时 `TARGET_REMAP` 硬编码迁移到 `instrument_rules.json.targetRemap`，降低后续规则漂移风险。
- 已开始直接升级策略层，使其更贴近“短线激进但不盲目追涨”的要求：
  - `gate_scoring.py` 新增强势切换识别、分级仓位建议（A/B/C tiers）、更贴近短线博弈的 momentum 评分；
  - `execute_gate_script_only.py` 新增双通道买入（回撤低吸 + 强势切换），买入仓位不再固定 5%，而是读取 evidence 中的 `suggestedBuyPct`；
  - 卖出目标从“仅按最差盈亏比”升级为“预期失败优先卖”，综合候选池在榜情况、置信度、动量与相对弱势；
  - 保留 stale/freshness/人工确认边界，不让系统为追求激进而越权。
- 已新增 `基金挑战#09-01:20策略复盘优化` isolated cron：每天凌晨自动复盘、学习、做低风险 challenge 内部优化，并提交 memory/fund_challenge 相关变更；真实外部买卖仍不绕过人工确认边界。
- 用户进一步明确：基金挑战策略层完全放权，接受极高风险甚至全部亏损；后续默认由助手主动执行策略、复盘、学习与优化，不再等待逐项授权。
- 文档规范补充：所有升级日志应统一放在 `docs/upgrades/YYYY-MM-DD/upgrade-log.en.md` 与 `upgrade-log.zh-CN.md`，不应放在仓库根目录。
- GitHub 推送习惯更新：推送时优先提交正式项目改动，不把明显本地缓存、运行时垃圾、临时脚本和环境残留直接裸推到远端；已开始用 `.gitignore` 持续收敛。
- README 维护方向已明确：需要持续反映真实 cron 调度、自主优化能力，以及“策略层 AI 全权处理、外部真实执行仍保留最终确认边界”的当前运行模式。
- 安全事故记录：发现 `memory/archive/2026-02-27.md` 曾包含 Telegram bot token，已完成三步处置：1) 当前文件 redaction；2) Git 历史重写并强推覆盖；3) 新增 secret handling 规则，后续 memory 只允许记录“已配置成功”，禁止记录密钥字面值。
