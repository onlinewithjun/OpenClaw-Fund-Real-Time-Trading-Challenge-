# 飞书机器人创建指南 - OpenClaw 集成

## 第一步：打开飞书开放平台
1. 已为您打开 Chrome 浏览器，访问：https://open.feishu.cn/app
2. 使用您的飞书账号登录（需企业管理员权限）

## 第二步：创建企业自建应用
1. 点击页面右上角 **「创建应用」** 按钮
2. 选择 **「企业自建应用」**
3. 填写应用信息：
   - **应用名称**：OpenClaw Bot（可自定义）
   - **应用描述**：OpenClaw AI助手飞书机器人
   - **应用图标**：（可选）可上传OpenClaw图标
   - **归属部门**：选择您的部门
4. 点击 **「确认创建」**

## 第三步：获取核心凭证
应用创建后，在左侧菜单栏找到 **「凭证与基础信息」**：
1. **App ID** - 复制并保存
2. **App Secret** - 点击「重置」生成新密钥，复制并保存

## 第四步：配置权限
在左侧菜单栏进入 **「权限管理」**：
1. 点击 **「添加权限」**
2. 搜索并添加以下 **必备权限**：
   - `im:message` - 获取与发送单聊、群聊消息
   - `im:message.group_at_msg` - 接收群聊中@机器人的消息
   - `im:message.p2p_msg` - 接收单聊消息
3. （可选）添加 **推荐权限**：
   - `contact:user.base` - 获取用户基本信息
   - `contact:user.email:readonly` - 获取用户邮箱

## 第五步：配置事件订阅（WebSocket模式推荐）
在左侧菜单栏进入 **「事件订阅」**：
1. 选择 **「WebSocket」** 连接模式（推荐，无需公网）
2. 系统自动生成 WebSocket 连接信息，OpenClaw 会自动使用

### 备选方案：Webhook模式（如需）
1. 选择 **「Webhook」** 模式
2. 获取以下信息：
   - `verification_token` - 校验令牌
   - `encrypt_key` - 加密密钥（可选）
3. 请求URL需填写 OpenClaw 的公网地址

## 第六步：版本管理与发布
在左侧菜单栏进入 **「版本管理与发布」**：
1. 点击 **「创建版本」**
2. 填写版本信息：
   - 版本号：1.0.0
   - 更新说明：初始版本
3. 选择可用人员范围（建议先选择自己测试）
4. 点击 **「保存」** → **「申请发布」**
5. 等待审核通过（一般较快）

## 第七步：OpenClaw 配置
获取以下凭证后，在终端运行：

```bash
openclaw channels add --channel feishu
```

按提示输入：
- `appId` = 您的App ID
- `appSecret` = 您的App Secret
- `domain` = "feishu"（默认）
- 其他凭证（如有）

或编辑配置文件 `C:\Users\Administrator\.openclaw\openclaw.json`：

```json
"channels": {
  "feishu": {
    "appId": "您的App ID",
    "appSecret": "您的App Secret",
    "domain": "feishu",
    "enabled": true
  }
}
```

## 第八步：测试连接
1. 重启 OpenClaw Gateway：
   ```bash
   openclaw gateway restart
   ```
2. 测试连接状态：
   ```bash
   openclaw channels status --probe
   ```
3. 在飞书中搜索您的应用名称，发送消息测试

## 故障排除

### 常见问题
1. **权限不足**：确保有企业管理员权限
2. **应用未发布**：版本必须发布才能生效
3. **凭证错误**：确认App ID和Secret正确
4. **网络问题**：确保能访问飞书服务器

### 获取帮助
- 飞书开放平台文档：https://open.feishu.cn/document/
- OpenClaw飞书插件文档：https://docs.openclaw.ai/channels/feishu

## 凭证记录表
请将获取的凭证记录在此：

| 凭证名称 | 值 | 获取位置 |
|---------|-----|---------|
| App ID | | 凭证与基础信息 |
| App Secret | | 凭证与基础信息 |
| verification_token | | 事件订阅（Webhook模式） |
| encrypt_key | | 事件订阅（Webhook模式） |

**注意**：App Secret 类似密码，请妥善保管！

---

完成上述步骤后，您的 OpenClaw 即可通过飞书机器人进行对话。